{
  "name": "@sulacharts/angular",
  "version": "0.1.0",
  "description": "Angular wrapper for SulaCharts — a standalone component that runs the core engine's DOM/event work outside Angular's zone (see chart.component.ts) and re-enters it only to emit @Output events. Still built with tsup as a placeholder; publishing this for real needs ng-packagr, tracked in PROGRESS.md — held back from the initial npm publish for that reason.",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": ["dist"],
  "license": "MIT",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "peerDependencies": {
    "@angular/core": ">=18.0.0"
  },
  "dependencies": {
    "@sulacharts/core": "workspace:*"
  },
  "devDependencies": {
    "@angular/core": "^18.2.0",
    "happy-dom": "^15.11.7",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
