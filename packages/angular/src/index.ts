// Angular wrapper (standalone component, ElementRef + ngOnChanges/ngOnDestroy)
// is scheduled for Day 5 — see PROGRESS.md. Depends on the core engine's
// render/update logic (Day 1-2) existing first. Will likely move from this
// placeholder tsup build to ng-packagr once real Angular decorators/DI are
// involved.
export {};
